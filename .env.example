# World Cup 2026 Travel Planner — Environment Configuration

# Default: SQLite (works out of the box, no setup required)
# The database file will be created automatically when you run the seed script.

# For PostgreSQL: uncomment the line below and run `docker-compose up -d`
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/worldcup
